// Shared between layout.tsx (the recorder header's "MAX 2:00" label) and,
// from stage 1b, useSpeechRecorder's auto-stop.
export const MAX_DURATION_SECONDS = 120;

export const MAX_TRANSCRIPT_CHARACTERS = 500;
